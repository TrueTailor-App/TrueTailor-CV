# TrueTailor CV — תיק פרויקט

מסמך ידע יחיד לפרויקט. נועד להיטען ל־Project Knowledge כדי שכל שיחה חדשה תתחיל עם ההקשר המלא במקום לבנות אותו מחדש.

עודכן: 27 ביולי 2026. גרסת קוד נוכחית: **v31.0**. קובץ: `TrueTailor_CV_v31.html`, 8,207 שורות.

---

## 1. מה זה

כלי בקובץ HTML יחיד שמתאים קורות חיים קיימים למודעת דרושים ספציפית. רץ כולו בדפדפן, בלי שרת ובלי בסיס נתונים. הקריאות היחידות החוצה הן ל־Google Gemini API, עם מפתח שהמשתמש מספק ונשמר ב־`localStorage` שלו בלבד.

ממשק בעברית או באנגלית לפי בחירת המשתמש (v31), תוכן קורות החיים נתמך בעברית ובאנגלית, וכל שורה שומרת על השפה שלה. שפת הממשק ושפת המסמך הן שני דברים נפרדים ואף אחת מהן לא משפיעה על השנייה.

המשתמש היחיד כיום הוא בעל הפרויקט.

## 2. עקרונות מוצר, לא לשנות בלי החלטה מפורשת

אלה הכללים שהמוצר בנוי סביבם. כל בקשה שסותרת אותם צריכה לעלות לדיון ולא להיות מיושמת בשקט.

1. **בלי המצאות.** אסור להוסיף כישור, כלי, טכנולוגיה, הסמכה, מעסיק, אחריות, מדד או הישג שלא מופיעים בקורות החיים המקוריים או שהמשתמש לא אמר בשיחה.
2. **אסור לחשב שנות ניסיון.** גם כשטווחי התאריכים מאפשרים זאת.
3. **אסור לשדרג שפת תמיכה לשפת ניהול.** "השתתפתי" לא הופך ל"ניהלתי", גם כשהמשרה מבקשת מנהל.
4. **אסור למחוק או לעגל מדד כמותי.** אחוזים, סכומים, נפחים, SLA, מספר משתמשים.
5. **ראיה מקבילה מותרת, שם הדבר החסר אסור.** אפשר לתאר את מה שיש בעזרת המאפיינים שהוא באמת חולק עם הדרישה (אותו סדר גודל, אותה סביבה מפוקחת, אותו סוג מערכת). אסור לנקוב במונח החסר, ואסור "בדומה ל", "מקביל ל", "כגון".
6. **מילת המפתח של המשרה נכנסת רק כשהמועמד באמת עשה את הדבר.** מצב `direct` בלבד. מצב `equivalent` מקבל את המאפיינים המשותפים בניסוח המשרה, לא את המילה.
7. **כל שינוי מאושר על ידי המשתמש, שורה אחר שורה.** שום דבר לא נכנס אוטומטית.
8. **מנעול שפה.** שורה שנכתבה מחדש חייבת להישאר בשפת השורה המקורית. שם מוצר או כלי באנגלית בתוך משפט עברי מותר. משפט שנכתב כולו בשפה השנייה נזרק בקוד.
9. **סקציות מחוץ להישג יד המודל:** השכלה, שפות, שירות צבאי, וכותרת המסמך (שם ופרטי קשר). לתואר, לדרגה ולטווח תאריכים אין מה להתאים ויש הרבה מה להמציא.
10. **בלי מקפים בפלט המודל,** בלי em dash, בלי מרכאות מסולסלות, בלי אימוג'י. אלה הסימנים שמסגירים כתיבת מכונה.
11. **ATS מעל אסתטיקה.** עמודה אחת של פסקאות רגילות. בלי טבלאות, בלי תיבות טקסט, בלי כותרות עליונות ותחתונות, בלי גרפיקה.
13. **שפת הממשק לא נוגעת במודל ולא במסמך.** (נוסף ב־v31) המתג מחליף טקסט של הממשק ואת כיוון המעטפת בלבד. הפרומפטים מקבלים `'he'` בכל מקרה, `APP.chat.lang` נגזר משפת קורות החיים, ו־`#previewPage` ו־`#printArea` כותבים כיווניות משלהם.
12. **רוחב הדומיין נבחר על ידי המשתמש, לא על ידי הכלי.** (נוסף ב־v29) כשקיים פער מינוח סקטוריאלי בין המשרה לקורות החיים, הכלי מציג אותו ושואל. הוא לא מרחיב את המינוח מיוזמתו ולא מקבע את המשתמש בתחום שלו מיוזמתו. ההרחבה משנה **ניסוח ותעדוף שאלות בלבד**, ולעולם לא את הטענה: מונח מקורי לא נמחק, ומערכת שלא הופעלה לא נכנסת.

## 3. ארכיטקטורה

- קובץ HTML יחיד, 7,495 שורות. סקריפט קלאסי בכוונה: כל הצהרת פונקציה ברמה העליונה הופכת לתכונה של `window`, כדי שאפשר יהיה להחליף `window.callGemini` בבדיקות.
- מצב גלובלי אחד: `APP`. מכיל את המסמך המפורק, פלט כל שלב, השיחה, ההצעות והטקסט הסופי.
- ספריות חיצוניות מ־CDN: Tailwind (CDN build), JSZip, Mammoth (קריאת docx), pdf.js (קריאת pdf), docx v9.7.1 (כתיבת Word).
- אין build step. עורכים את הקובץ, פותחים בדפדפן.

### שכבת המודל
- `TT_MODEL_CASCADE` מונה מודלים לפי סדר עדיפות, ו־`discoverModels()` מצליב אותו מול מה שהחשבון באמת מחזיק, עם מטמון של 24 שעות ב־`localStorage`. רשימות מקובעות מתיישנות: `gemini-2.0-flash` הושבת ב־1 ביוני 2026.
- `fetchWithRetry()` עם exponential backoff.
- `parseGeminiJSON()` משותף, מסיר גדרות markdown ומתקן פסיקים תלויים.
- `TT_TOKENS` קובע תקרת פלט לכל שלב: signals 16000, coverage 24000, synthesis 16000, skills 8000, spell 4000, qtranslate 4000, answerSpell 1500, question 4000.
- `ttPromptJoin(parts)` (נוסף ב־v29) מחבר חלקי פרומפט ומשמיט `null`. בלוק שלא רלוונטי למצב הנוכחי מוחזר כ־`null` ולא כמחרוזת ריקה, כדי שלא יישאר אחריו אפילו שורה ריקה. זו הדרך שבה מצב `strict` נשאר זהה בית מול בית לגרסה הקודמת.

## 4. הצינור, ששה שלבים ושער אחד באמצע

| שלב | מה קורה | פונקציות מפתח |
|---|---|---|
| 1 | פירוק המשרה לדרישות ממוספרות עם מילות מפתח, must_have ומשקל | `stepExtractSignals`, `promptSignals`, `normalizeSignals` |
| 2 | השוואה דרישה מול דרישה מול קורות החיים, וייצור ממצאים | `stepCoverage`, `promptCoverage` |
| 2b | שער רוחב הדומיין. נפתח רק כשיש פער מינוח סקטוריאלי | `domainGapReport`, `renderDomainGate`, `chooseDomainMode`, `continueAfterRelevance` |
| 3 | שיחת שאלות התאמה ממוקדת, נגזרת מהפערים לפי סדר חשיבות | `buildQueue`, `askNextQuestion`, `submitAnswer` |
| 4 | שכתוב סקציה אחת בכל פעם, פלוס תוכנית לסקציית הכישורים | `runSynthesis`, `promptSynthesis`, `promptSkillPlan` |
| 5 | אישור, דחייה או עריכה של כל הצעה בנפרד | `openReview`, `renderReview`, `compileApproved` |
| 6 | תצוגה מקדימה שהיא המסמך עצמו, עריכה במקום, ייצוא | `renderPreview`, `exportDocx`, `exportPdf` |
| 7 | גיליון הכנה לראיון, למועמד בלבד, אפס קריאות מודל | `interviewSheet`, `renderInterviewSheet`, `interviewSheetText`, `blockRoleLabel` |

בין 2 ל־3 יש שני שערים, בסדר הזה:

1. `relevanceVerdict()` עוצר כשאין שום קשר בין קורות החיים למשרה. שלושה תנאים חייבים להיכשל יחד: אף דרישה לא הוכחה, לכל היותר דרישה אחת נגועה חלקית, וכיסוי מילות המפתח מתחת ל־12 אחוז. אפשר להמשיך בכל זאת בלחיצה.
2. שער רוחב הדומיין, ראה סעיף 4a. הוא נמצא אחרי שער הרלוונטיות, כי אין טעם לבחור אוצר מילים לקורות חיים שאין להם עסק עם המשרה הזו מלכתחילה.

`continueAfterRelevance()` היא הדרך היחידה לשלב 3, גם מכפתור "המשך בכל זאת" של מסך אין קשר, כדי שאי אפשר יהיה לעקוף את השער.

מכסת השאלות: `Math.max(6, Math.min(16, queue.length))`.

### 4a. שער רוחב הדומיין (v29)

**הבעיה.** System Owner בייצור ו־System Owner בבנק עושים את אותה עבודה עם שמות עצם שונים. שלב 2 כבר מזהה את זה: דרישה חוזרת כ־`equivalent` בדיוק כשקורות החיים לא נוקבים בשם הדבר אבל מוכיחים את המאפיינים שהופכים אותו לחשוב. ערימה של אלה היא לא מועמד חלש, היא מועמד שנקרא באוצר המילים של התעשייה הלא נכונה.

**המדידה, בקוד ולא במודל.** `domainGapReport()` סופר את הדרישות שחזרו `equivalent`, כמה מהן `must_have`, ואוסף את ה־`shared_properties` שלהן. אפס קריאות API. השער נפתח כששתי דרישות כאלה קיימות, או אחת שהיא חובה (`TT_DOMAIN_MIN_EQUIV = 2`).

**למה כאן ולא קודם.** לפני שלב 2 אין פער קונקרטי להציג, יש רק את הדומיין שהמשרה הצהירה עליו. לפני שלב 3 עוד לא נשאלה שאלה ולא נכתבה מילה, ולכן שום דבר לא צריך להתבטל אחרי הבחירה.

**שני המצבים.** `APP.domainMode`:

| ערך | מתי | מה זה עושה |
|---|---|---|
| `''` | השער לא נפתח | מתנהג בדיוק כמו `strict` בכל מקום |
| `strict` | המשתמש בחר להישאר בתחומו | אפס שינוי מול v28.1. אין תוספת פרומפט ואין שינוי בתור |
| `broad` | המשתמש בחר להרחיב | ראה שתי השורות הבאות |

**מה `broad` משנה, שני דברים בלבד:**
1. `buildQueue()` מעלה פריטי `equivalent` מתחתית התור לראשו (`base` מ־1 ל־3), ומכניס גם פריטים ש־`ask_user` שלהם `false`. אותה פסיקה התקבלה בקריאה צרה של קורות החיים, ולכן היא לא מחייבת במצב הרחב.
2. `domainModeBlock()` מוסיף את כללי X1 עד X6 ל־`promptSynthesis` ול־`promptSkillPlan`, ובלוק הנחיה מקביל נכנס ל־`chatSystemInstruction`.

**כלל X2, הכלל שהוחלט עליו במפורש: מוסיפים, לא מחליפים.** "MES" הופך ל"MES, מערכת ליבה תפעולית לניהול תהליכים בזמן אמת". הוא לא הופך ל"מערכת ליבה תפעולית" לבדו. מחיקת השם המקורי סוחרת מונח אמיתי שסורק ATS מחפש תמורת מונח מעורפל שאף אחד לא מחפש, וזה ההפך מהמטרה. X3 אוסר את המחיקה, X4 חוזר במפורש שכללים 1 עד 8 של `HARD_RULES` לא זזו, X5 מגביל את ההכללה ללייבל של המערכת ולא לעובדות סביבה.

**טבלת ההשוואה קופאת, וזה מוצהר.** מצב `broad` לא מריץ מחדש את שלב 2. הכרטיס נשאר על המסך אחרי הבחירה, מכווץ לשורה אחת שאומרת באיזה מצב ההרצה ושהטבלה מציגה את ההשוואה הצרה. זו החלטה מודעת: המספר בטבלה הוא מה שהוכח, לא מה שאפשר לנסח.

**המצב שייך להרצה אחת.** `resetRun()` מנקה אותו. מצב ששרד להרצה הבאה היה עונה בשקט על שאלה שהמועמד לא נשאל, על משרה שהוא עוד לא ראה.

### 4b. גיליון ההכנה לראיון (v30)

**מה זה.** כרטיס אחרון בעמוד, אחרי התצוגה המקדימה וההורדה. שלושה חלקים: אילו סעיפים עלו לראש כל תפקיד ולמה, מה לספר כששואלים על היום יום, ואילו דרישות של המשרה נשארו בלי כיסוי.

**אפס קריאות מודל.** זו ההחלטה המרכזית כאן, משתי סיבות. מודל שנשאל מה המועמד צריך לספר בראיון עוזר בדרך שהקובץ הזה קיים כדי למנוע, כלומר בהמצאה. ובנוסף שלושת החלקים הם חיפוש, חיפוש וסינון מעל מצב שכבר קיים: `APP.changes` מסוג `line_order` מחזיק את ההעלאות ואת הנימוק, ו־`APP.coverage` כבר יודע מה לא הוכח. לפי סעיף 6, שיפוט שמצטמצם לאריתמטיקה יושב בקוד ולא בפרומפט.

**הפרדה מהקובץ המיוצא.** הגיליון לא נכנס לקורות החיים בשום צורה. שלוש שכבות: נתיב ה־PDF מסתיר כל מה שמחוץ ל־`#printArea`, נתיב ה־Word נבנה מ־`APP.finalText` שהכרטיס לא נוגע בו, ו־`tt-noprint` על הכרטיס הוא המנעול השלישי ולא הראשון.

**חלק 3 מאבחן ולא מציע.** הוא לא מנסח שורה, לא מרמז איך לכסות פער ולא מציע ניסוח למסמך. הוא גם מפריד בין שני סוגי פער: `missing` הוא פער אמיתי, ו־`equivalent` על דרישת חובה הוא פער שם בלבד, כלומר המועמד מוכיח את התוכן ולא את המונח. ההפרדה הזו כבר קיימת במפת הכיסוי והמסך רק חושף אותה.

**"התפקיד הראשון" ולא "התפקיד הנוכחי".** חלק 2 נקרא מבלוק הניסיון הראשון בעמוד. הקובץ לא קורא תאריכים, והסקת "זה התפקיד הנוכחי" ממיקום היא בדיוק סוג ההסקה שהמוצר מסרב לעשות, ולכן גם הטקסט על המסך אומר "התפקיד הראשון בסעיף הניסיון".

### 4c. שפת הממשק ואודות (v31)

**מה המתג עושה.** `applyUiLang(lang)` מחליף טקסט של אלמנטים סטטיים לפי טבלת `TT_UI_STRINGS` ומעדכן `dir` ו־`lang` על `<html>`. הבחירה נשמרת ב־`localStorage` תחת `tt_ui_lang`, ברירת המחדל עברית.

**מה הוא לא עושה.** לא נוגע בפרומפטים: `promptCoverage` ו־`promptSkillPlan` מקבלים `'he'` כליטרל בשני אתרי הקריאה, ו־`APP.chat.lang` נגזר מ־`docLanguage(APP.doc)`. לא נוגע במסמך: `#previewPage` ו־`#printArea` כותבים `style.direction` inline, וכיווניות inline גוברת על ירושה.

**כיוון המעטפת כן משתנה, וזו חריגה מודעת מ"רק הטקסט".** טקסט אנגלי בתוך מיכל RTL זורק פסיק ונקודה לקצה הלא נכון של כל שורה. השארת המעטפת RTL לא הייתה "רק הטקסט השתנה", היא הייתה "הטקסט השתנה ונשבר".

**הצד העברי של הטבלה לא כתוב בשום מקום.** הוא נקרא מה־DOM בקריאה הראשונה ל־`applyUiLang` ונשמר ב־`TT_UI_HE`. המשמעות: טעות בתרגום לא יכולה לפגוע בממשק העברי, וסלקטור שהפסיק להתאים עולה מחרוזת אנגלית ולא מחרוזת עברית. `checkUiStrings()` רץ באתחול ומדווח לקונסולה על כל סלקטור שלא מתאים לאלמנט אחד בדיוק.

**מה לא בטבלה.** כל אלמנט ש־JS כותב לתוכו בזמן ריצה: `#globalStatus`, `#keyStatus`, `#qCount`, `#qHint`, `#finalViewNote`, `#bulletToggleNote`. טבלת תרגום שנלחמת ברנדרר מפסידה לפי סדר ההרצה. הפועל היוצא הוא שמחרוזות סטטוס ושגיאה נשארות עברית גם במצב אנגלית, ראה סעיף 10.

**הדגלים הם SVG ולא אימוג'י.** ל־Windows אין גליף לצמד regional indicator, ולכן דגל אימוג'י מוצג שם כשתי אותיות. כפתור דגל שמציג אותיות הוא לא כפתור דגל.

**כרטיס אודות.** `renderAbout()` בונה את התוכן בשתי השפות, כולל `TT_VERSION` החי. סעיף הפרטיות אומר רק מה שהקובץ יכול להבטיח: אין שרת, המפתח ב־`localStorage`, והטקסט נשלח ל־Gemini עם המפתח של המשתמש. מה שגוגל עושה בטקסט נקבע בתנאי המסלול של המפתח, והכרטיס אומר זאת במפורש במקום להבטיח במקומה. הנוסח המקורי שהוצע הכיל התחייבות שהאפליקציה לא יכולה לגבות, והיא הוחלפה.

## 5. מודל הנתונים

`parseResume(text)` מייצר:
- `doc.lines[]` — כל שורה עם `id` (L0, L1, ...), `raw`, `marker` (תו התבליט), `text`, `type` (blank / bullet / prose / heading), `subtype` (roleheader), `cat` (header / summary / experience / skills / education / other ...), `isHeading`, `editable`, `lang`.
- `doc.sections[]` — אינדקס סקציות.
- `doc.blocks[]` — רצף שורות עריכות ללא הפסקה. כותרת, שורה ריקה או שורת חברה־תאריכים מסיימות בלוק. זו יחידת סידור מחדש, והיא מה שמונע העברת תבליט מתפקיד אחד לתפקיד אחר.

`EDITABLE_CATEGORIES = { summary, experience, skills, other }`.

`APP.domainMode` (v29): `''`, `'strict'` או `'broad'`. `APP.domainGap` שומר את הדוח שהשער חושב ממנו.

הצעות שינוי (`APP.changes[]`), כל אחת עם `id`, `kind`, `cat`, `lineId`, `before`, `after`, `why`, `reqIds`, `status`. סוגי `kind`: `line`, `add`, `line_order`, `skill_rename`, `skill_drop`, `skill_add`, `skill_order`.

`rebuildDocument()` בונה את הטקסט הסופי מהמבנה המאושר: סקציית הכישורים מוחלפת כמקשה אחת, בלוק שסודר מחדש נכתב בסדר החדש, ותבליט שנוסף נוסע עם השורה שאליה הוצמד.

## 6. מנגנוני הגנה בקוד, לא בפרומפט

- `enforceLanguageLock` / `languageLockVerdict` — זורק ניסוח שנכתב בשפה הלא נכונה. כל דחייה נרשמת ב־`APP.langLockLog` ומוצגת למשתמש, כי דחייה היא החלטה שהאפליקציה קיבלה במקומו.
- `offensiveWordsAdded` — ניסוח שהכניס מילה פוגענית שלא הייתה במקור נזרק כולו.
- `sanitizeAiText` / `stripAiTypography` / `stripHyphens` — מסיר מקפים, em dash, מרכאות מסולסלות ואוצר מילים של מודלים.
- `carryEmphasis` — מודגש, נטוי וקו תחתון שנקראו מהקובץ המקורי נשמרים גם אחרי שהשורה נכתבה מחדש.
- `keywordReport` / `keywordBaseReport` — מדד כיסוי דטרמיניסטי, מחושב בדפדפן. המודל לא מדרג את עצמו.
- `atsAudit` / `atsScore` — בדיקת ATS מקומית עם השוואת לפני ואחרי.
- `proofread` / `scheduleSpellPass` — הגהה שרצה ברקע, וההורדה ממתינה לה.
- `domainGapReport` (v29) — זיהוי פער הדומיין הוא ספירה של סטטוסים, לא שיפוט של מודל.
- `interviewSheet` (v30) — גיליון ההכנה לראיון נבנה כולו בקוד ממצב קיים, אפס קריאות מודל. כל שורה בו נגזרת מ־`APP.changes`, `APP.coverage`, `APP.signals` או `APP.chat.qa`, ולכן אין בו מה להמציא.

## 7. שקלול מילות מפתח

`termWeight()` מחשב 0 עד 100 לכל מונח, מתוך המודעה עצמה: משקל שהמודל נתן לדרישה (עד 60), בונוס must_have, בונוס גדול אם המונח מופיע בכותרת התפקיד, בונוס אם הוא ברבע הפותח של המודעה, בונוס על חזרות, ועונש אם המודל המציא מונח שהמודעה בכלל לא הדפיסה.

`priorityKeywordBrief(limit)` מייצר את הרשימה שנמסרת לשלבי הכתיבה, כולל הוראה פרטנית לכל מונח לפי סטטוס הכיסוי שלו: להשתמש במילה, לכתוב רק את המאפיינים המשותפים, או לא לגעת בכלל.

`sectionKeywordGaps(sectionText)` (נוסף ב־v26) מחשב בדפדפן אילו מונחים מותרים עדיין לא מופיעים בסקציה הנוכחית, ומוסר את הרשימה למודל. זו אריתמטיקה ולא שיפוט, ולכן היא לא מופקדת בידי המודל.

## 8. ייצוא

- **Word**: `buildDocxXml` בונה XML ידנית, עם תמיכה בפסקאות RTL. שדרוג ספריית docx מ־v7.1.0 ל־v9.7.1 תיקן באג דו־כיווניות מאושר.
- **PDF**: `buildPrintHtml` פלוס print CSS. הכיווניות נכתבת שלוש פעמים בכוונה, כי גיליונות הדפסה נצפו מתעלמים מהתכונה.
- **תבניות**: `classic`, `executive`, `band`, `showcase`, ועוד תבנית שמתגלה בזמן ריצה מתוך הקובץ שהמשתמש העלה ומשחזרת את הגופן, הגדלים והצבע שלו. הגדרה אחת מזינה את התצוגה, את ה־Word ואת דף ההדפסה, ולכן שלושתם לא יכולים להתפצל.

## 9. יומן גרסאות

### לפני יולי 2026
- מודל Surgeon לאישור ברמת השורה, עם word diff מבוסס LCS לכל כרטיס.
- הגבלת סקציות: השכלה, שפות, צבא וכותרת מחוץ להישג יד.
- מודל הצעות JSON דליל.
- מערכת תרגום דו־מסלולית, לפני הניתוח ואחרי האישור.
- Adaptive Task Feed עם micro_action / deep_action / flag_sensitive_context.
- שאלון פערי כישורים עם תבניות רב־ברירה.
- מדד כיסוי מילות מפתח עם השוואת לפני ואחרי.
- סקציית שירות צבאי ושפות מקובעת, אחרי החלטה מודעת ומפורשת בבקשה השלישית.

### v25.8, 14 ביולי
פריסת עמוד רציף, תבנית ויזואלית נעולה, סקירה ברמת השורה, הדגשה חיה באישור, ייצוא DOCX עם שמירת מודגש ונטוי.

### v26.0, 26 ביולי
1. **חיווי שאלות**: "שאלה 4 מתוך 6", פס התקדמות ו־pip לכל שאלה. `renderChatProgress()`.
2. **קונסולת סטטוס נעוצה לתחתית החלון**, כי כל קריאה ארוכה קורית אחרי שהמשתמש גלל. `setConsole()`, `setStage()`, פס שלבים בראש העמוד.
3. **שלב 5**: כפתור "המשך לתצוגה" הועבר לסוף השלב, בתוך `.tt-stepfoot` עם סיכום כמה הצעות יאושרו.
4. **תצוגת "מה השתנה" נכתבה מחדש** ומוצגת בתוך מבנה קורות החיים: כותרות סקציה עם מונה שינויים, שורות תפקיד, תבליטים, פס בצד לשורה שהשתנתה, תג לשורה שהועברה, והצעות שנדחו בקו חוצה. המעבר על המסמך זהה לזה של `rebuildDocument`, כדי שהתצוגה לא תוכל לתאר מסמך אחר מזה שיורד.
5. **תצוגת הטקסט הגולמי** קיבלה מתג בין מבנה לטקסט, עם מבנה כברירת מחדל. `renderFinalStruct()`.
6. **דיוק מול המשרה**: רשימת מילות המפתח לשלבי הכתיבה גדלה מ־14 ל־20, נוספה `sectionKeywordGaps()`, ונוספו כללים D3 עד D5 (החלפת מונח לשוני כשהעובדה כבר מוכחת, איסור חזרה על מונח שכבר קיים בסקציה) וכלל מקביל לסקציית הכישורים.

### v27.0, 26 ביולי
שכבת עיצוב חדשה לגמרי, אחרי ש־v26 לא נראה שונה בפועל.
- **סיבת הכשל**: `<body class="bg-slate-100 text-slate-800">` נשאר במקום. מחלקת Tailwind גוברת על סלקטור אלמנט, ולכן `body { background }` לא נכנס לתוקף. בנוסף, ה־CDN של Tailwind מזריק את הגיליון שלו ל־head בזמן ריצה, כלומר אחרי בלוק ה־style של הקובץ. **כל כלל שחייב לנצח נכתב מאז כ־`html body` או נושא מחלקה.**
- כיוון: מעטפת כהה, גיליונות לבנים. סרגל עליון דביק עם מונוגרמה ופס שלבים, כותרת פתיחה, כרטיסים לבנים מוגבהים.
- שתי משפחות צבע עם תפקידים: טורקיז פועל (כפתורים, מיקוד, שורות שהשתנו), פליז סופר (פס השלבים, מד השאלות).
- גופנים: Heebo ל־display, Assistant לגוף.
- תיקונים נגררים: המודל של "איך משיגים מפתח" ירש צבע טקסט בהיר על רקע לבן, והצל של דף התצוגה הוגבל ל־`.tt-pagewrap` כדי שלא יזלוג ל־`#printArea` ויודפס ל־PDF.

### v28.0 ו־v28.1
**לא תועדו בזמן אמת.** התיק נשאר על v27.0 בזמן ששתי המסירות האלה נכנסו לקוד. מה שקיים ב־v28.1 ואינו מתואר בתיק v27, לפי קריאה בקוד ולא לפי יומן:
- מערכת הגהה לתשובות המשתמש בשיחה: `checkAnswerSpelling`, `runAnswerSpell`, `renderAnswerSpell`, `promptAnswerSpell`, `answerLayerHtml`, שכבת `.tt-spelllayer` מעל תיבת התשובה.
- דוח `answerUse`: `collectAnswerUse` / `renderAnswerUse`, מה כל תשובה באמת שינתה.
- חזרה אחורה וניסיון חוזר בשיחה: `rewindChatTurn`, `showChatRetry`.
- מתג תבליטים: `toggleBullets`, `bulletState`, `applyBulletMarkers`, `syncBulletToggle`.
- התראת מדדים כמותיים חד פעמית: `hasMeasurableAchievement`.
- זיהוי תבנית מתוך PDF שהועלה: `templateFromPdfStyle`, `pdfFontMark`, `cleanFontName`.
- `TT_TOKENS` גדל משישה שדות לשמונה, כולל `question: 4000` שמתקן שלב 3 שחזר ריק.

הרשימה הזו היא הסקה מהקוד. אין דרך לאמת אותה בלי diff מול `TrueTailor_CV_v27.html`.

### v29.0, 26 ביולי
שער רוחב הדומיין, ושינוי שמות שני שלבים.
1. **שער רוחב הדומיין** בין שלב 2 לשלב 3, מפורט בסעיף 4a. `domainGapReport`, `renderDomainGate`, `chooseDomainMode`, `continueAfterRelevance`, `domainModeBlock`, כרטיס `#domainCard`, עקרון מוצר 12.
2. **`ttPromptJoin()`** ו־`null` כסמן להיעדרות בפרומפטים. הסיבה: בלוק שהוחזר כמחרוזת ריקה השאיר שורה ריקה בפרומפט, כלומר מצב `strict` היה שונה מ־v28.1 בתו אחד. פרומפט ששונה מהקודם בשורה ריקה תועה הוא פרומפט שאף אחד לא באמת בדק את ההתנהגות שלו.
3. **שמות שלבים**: ברצועה העליונה "שאלות" הפך ל"שאלות התאמה" ו"כתיבה" הפך ל"שכתוב מחדש". אותם שמות עודכנו ב־`TT_STAGE_NAMES` (הקונסולה התחתונה), בכותרת כרטיס שלב 3, ובמשפט הסבר אחד במד מילות המפתח.
4. **מה שנבדק**: 83 בדיקות ב־jsdom, קובץ `tt_domain_tests.js`. כולל השוואה בית מול בית של תור השאלות, פרומפט השאלות ופרומפט השכתוב בין מצב `strict` לבין `TrueTailor_CV_v28_1.html` עצמו. שלושתם זהים.
5. **מה שלא נבדק**: ריצה מול Gemini API, ייצוא Word, ייצוא PDF.

### v30.0, 27 ביולי
גיליון הכנה לראיון, שלב 7. מפורט בסעיף 4b.
1. **כרטיס `#interviewCard`** אחרי `previewCard`, נחשף יחד איתו. `interviewSheet`, `renderInterviewSheet`, `interviewSheetText`, `copyInterviewSheet`, `blockRoleLabel`, `prepLineText`, `prepNote`.
2. **נבנה בזנב של `renderPreview()`**, כלומר מתעדכן בכל מקום שבו הטקסט הסופי מתעדכן. עלות אפס, שום קריאה לא יוצאת מהדפדפן.
3. **`resetRun()`** מסתיר ומרוקן את הכרטיס, כמו כל שאר הכרטיסים.
4. **מה שנבדק**: טעינה ב־jsdom בלי שגיאות; 27 בדיקות על הגיליון עצמו (זיהוי העלאות עם מיקום לפני ואחרי, שימוש בטקסט המאושר ולא במקורי, מיפוי מזהה דרישה לטקסט, מיון חובה קודם, הפרדת `missing` מ־`equivalent`, החרגת תשובה שדולגה, מצב ריק, מצב בלי טקסט סופי); ובדיקת אי דליפה: `APP.finalText` לא זז, הכרטיס מחוץ ל־`#printArea` ומחוץ ל־`#previewPage` ונושא `tt-noprint`. בנוסף השוואה בית מול בית מול `TrueTailor_CV_v29_1.html` של `promptSynthesis`, `promptSkillPlan`, `buildQueue`, `HARD_RULES` ו־`domainModeBlock` במצב `strict`, כולם זהים.
5. **מה שלא נבדק**: ריצה מול Gemini API, ייצוא Word, ייצוא PDF, וכפתור ההעתקה מול לוח הגזירים האמיתי של דפדפן.
6. **מה שלא נכנס בכוונה**: מחיקת תבליט לא רלוונטי. ראה סעיף 11.

### v31.0, 27 ביולי
מתג שפת ממשק וכרטיס אודות. מפורט בסעיף 4c.
1. **מתג שפה** בראש העמוד, עברית ואנגלית, דגלי SVG. `TT_UI_LANG`, `TT_UI_STRINGS`, `TT_UI_HE`, `applyUiLang`, `checkUiStrings`, `L(he,en)`.
2. **כרטיס אודות** `#aboutModal`. `renderAbout`, `aboutNote`, `TT_CONTACT`. כפתור משוב פותח `mailto:`, כפתור שיתוף מעתיק טקסט ואומר שאין עדיין כתובת ציבורית.
3. **עיקרון מוצר 13** נוסף לסעיף 2.
4. **מה שנבדק**: 31 בדיקות על שכבת הממשק, כולל שחזור בית מול בית של המעטפת העברית אחרי מעבר לאנגלית וחזרה, זהות `promptSynthesis` בין שתי שפות הממשק, `APP.chat.lang` שלא זז, `#previewPage` ששומר על הכיווניות שלו, `APP.finalText` שלא זז, כל סלקטור בטבלה מתאים לאלמנט אחד בדיוק, אין אימוג'י דגל בקובץ, ערך שפה לא מוכר נופל לעברית. בנוסף 27 הבדיקות של שלב 7 והשוואת המצב הצר מול `TrueTailor_CV_v29_1.html`, כולן עדיין עוברות. טעינה ב־jsdom בלי שגיאה ובלי אזהרה.
5. **מה שלא נבדק**: ריצה מול Gemini API, ייצוא Word, ייצוא PDF, ומראה בפועל של המעטפת ב־LTR בדפדפן אמיתי.

## 10. פערים ידועים

- לא נבדקה ריצה מלאה מול Gemini API מאז v25.8. הבדיקות שנעשו הן מבנה, לוגיקה ורינדור ב־jsdom.
- ייצוא Word ו־PDF לא נבדק מאז v25.8.
- **כלל X3 של המצב הרחב נאכף בפרומפט בלבד.** אין בקוד בדיקה שמונח מקורי לא נמחק מהשורה. זו חריגה מהעיקרון של סעיף 6, והיא מודעת: הבדיקה דורשת לדעת אילו מונחים בשורה הם שמות מערכת, וזו לא אריתמטיקה. אם המצב הרחב ייכנס לשימוש קבוע, זה המקום הראשון לחזק.
- טבלת ההשוואה במצב `broad` לא מחושבת מחדש. זו החלטה, לא באג, והמסך אומר אותה.
- אין חבילת בדיקות אוטומטית בתוך הקובץ עצמו. חבילת הבדיקות היא קובץ חיצוני: `tt_domain_tests.js` פלוס `tt_test_harness.js`, דורשת `npm install jsdom`.
- מדד כיסוי מילות המפתח חסום מלמעלה בכוונה: מונחים בסטטוס `missing` או `equivalent` לא ייכתבו לעולם. הדרך היחידה להעלות את האחוז היא לענות על יותר שאלות בשלב 3. זה לא באג.
- **תרגום הממשק חלקי בכוונה.** `TT_UI_STRINGS` מכסה את הטקסט הסטטי, כלומר את מה שנראה על המסך לפני שמשהו רץ. מחרוזות שנבנות ב־JS בזמן ריצה נשארות עברית: הודעות סטטוס, הודעות שגיאה, כרטיסי הסקירה בשלב 5, דוח מילות המפתח, דוח ה־ATS ותצוגת מה השתנה. במצב אנגלית הן יופיעו בעברית. הרחבה דורשת מעבר על כל אתר קריאה ל־`setStatus`, `showError` ולרנדררים של הדוחות, וזה שינוי גדול בהרבה מזה.
- כיוון המעטפת עובר ל־LTR במצב אנגלית. זה נבדק ב־jsdom ברמת התכונה, לא ברמת המראה בפועל בדפדפן. יש להסתכל על העמוד באנגלית לפני שמסתמכים עליו.
- הקובץ תלוי ב־CDN לטעינה ראשונה (Tailwind, גופנים, mammoth, pdf.js).
- המפתח API נשמר ב־`localStorage` לפי מקור הקובץ. פתיחה מתיקייה אחרת דורשת הדבקה מחדש.
- חבילת הבדיקות החיצונית `tt_domain_tests.js` לא הורצה מול v30.0, כי הקובץ לא היה זמין במסירה הזו. הבדיקות שדווחו לעיל נכתבו לצורך המסירה ורצו ב־jsdom.
- כפתור ההעתקה בשלב 7 נופל ל־`document.execCommand('copy')` כשאין `navigator.clipboard`, מה שקורה בפתיחה מ־`file://` בחלק מהדפדפנים. הנתיב הזה לא נבדק מול דפדפן אמיתי.

## 11. רעיונות שעלו ולא בוצעו

- **מחיקת תבליט שאינו רלוונטי למשרה.** הועלה במפורש ולא בוצע, כי הוא סותר כלל קיים: כלל J ב־`promptSynthesis` אומר "a line that proves nothing this posting asks for goes last, it does not get deleted". סעיף 12 אומר שבקשה שסותרת כלל קיים עולה לדיון ולא מיושמת בשקט. אם זה כן ייכנס, הוא דורש: הצעה מסוג `line_drop` שעוברת אישור פרטני כמו כל שינוי אחר לפי עיקרון 7, רצפה מינימלית של תבליטים לתפקיד, והגנה על התבליט היחיד שמגדיר את היקף התפקיד.

- אכיפה בקוד של כלל X3 (השם המקורי לא נמחק במצב הרחב).
- אפשרות שלישית בשער: להריץ מחדש את שלב 2 לפי המצב שנבחר, כדי שטבלת ההשוואה והציון יתעדכנו. נדחתה בגלל קריאת API נוספת והמתנה נוספת.
- לולאת משוב אחרי הכתיבה: לחשב אילו מילות מפתח מותרות עדיין חסרות בטקסט הסופי, ולהציע סבב כתיבה שני ממוקד.
- בדיקה אוטומטית מקצה לקצה מול מפתח API אמיתי.
- הורדת התלות ב־CDN לטובת קובץ עצמאי אמיתי.

## 12. מוסכמות עבודה בפרויקט

- שינוי מגיע כקובץ HTML שלם ומוכן להרצה, לא כתיקון חלקי.
- כל שינוי מלווה בציון מפורש של מה נבדק ומה לא נבדק.
- הערות בקוד מסבירות **למה** ההחלטה התקבלה, בעיקר במקומות שבהם משהו נכשל בעבר. זה הסגנון הקיים בקובץ וכדאי לשמר אותו.
- כשבקשה סותרת את עקרונות סעיף 2, אומרים זאת ולא מיישמים בשקט.
- **התיק מתעדכן באותה מסירה שבה הקוד משתנה.** v28.0 ו־v28.1 נכנסו בלי שהתיק זז, והתוצאה הייתה שיחה שלמה שנפתחה עם ידע שגוי על גרסת הקוד.
