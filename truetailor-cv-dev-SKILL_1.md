---
name: truetailor-cv-dev
description: Use when working on the TrueTailor CV project — a single-file HTML/JS resume-tailoring web app (TrueTailor_CV_vXX.html) that calls the Gemini API client-side. Load this before making any code change, bug fix, feature spec, or accessibility/dark-mode/i18n work on the file, and before writing a new session-summary doc for the project. Covers architecture conventions, the versioning scheme, the testing methodology (Playwright + local Tailwind build), recurring bug patterns to check for pre-emptively, and the standing product/legal decisions already made so they aren't re-litigated.
---

# TrueTailor CV — מדריך עבודה על הפרויקט

סקיל זה מסכם את כל מה שנלמד ונקבע בפרויקט TrueTailor CV עד כה (v39.33 → v39.45.3),
מתוך כל השיחות/הדוחות ששמורים בפרויקט. המטרה: session חדש על הקוד הזה לא צריך
לגלות שוב את אותם באגים, לא צריך לשאול שוב שאלות שכבר הוכרעו, ויודע איך לבדוק
לפני שהוא מציג גרסה כגמורה.

**קרא קודם את שלושת הפרקים הראשונים.** השאר הוא עיון לפי צורך.

---

## 0. אזהרה קריטית — הדוחות בפרויקט לא תמיד תואמים את הקובץ בפועל

נבדק ב-2026-08-22 מול הקובץ שהועלה בפועל (`TrueTailor_CV_v39_45_2.html`, `TT_VERSION`
זהה בקוד). התוצאה: **הדוח `gemini-transport.md` (v39.45 / v39.45.1) מתאר ענף
שכלל לא נכנס לקובץ הזה.** נבדק ישירות בגריפ על הקוד עצמו — כל אחד מהמושגים הבאים
שהדוח מתאר **נעדר לגמרי**: `TT_TIER`, `ttPinnedCall`, `ttReasonChat`, `ttThinkingFor`,
`MODEL_EXCLUDE_TEXT`, `TT_QUESTION_THINKING`, `respectRetryDelay`, `TT_VOICE_MIC_IDLE_MS`.
במקומם, הקובץ בפועל ממשיך עם המנגנון הישן יותר שמתועד ב-`model-selection-and-speed.md`
(`ttThinkConfigFor`, לא `ttThinkingFor`) ו-`transient-failures-and-selftest.md`
(`TT_RETRY_SAME` — ניסיון חוזר בודד לאותו מודל על 429/500/503, בלי כיבוד `Retry-After`).

**המשמעות המעשית שנבדקה ואומתה בקוד:**
- `ttVoiceStart` קורא ל-`getUserMedia` **בכל שאלה מחדש** — תיקון "החזקת המיקרופון
  בין שאלות" (gemini-transport.md §12ב) **לא קיים בפועל**. מי שעונה בקול על כמה
  שאלות רצופות רואה שוב את דיאלוג הרשאת המיקרופון בכל שאלה.
- אין הפרדת דרג `measure`/`reason`/`write`/`utility` ואין `respectRetryDelay` —
  כלומר תקלה רגעית (429/500/503) במודל החזק בשלב המדידה/השאלות עדיין יכולה
  "להחליק" את הציון למודל חלש יותר אחרי ניסיון חוזר בודד, כמתואר כבאג פתוח
  ב-`transient-failures-and-selftest.md`, **לא** כפי שנרשם כפתור ב-gemini-transport.md.
- **בלוק "VERSION HISTORY" בראש הקובץ נעצר ב-v39.44** (שורות ~46–133) ואינו
  משקף בכלל את v39.44.5 עד v39.45.2 — למרות ש-`TT_VERSION` וה-diff האמיתי בקוד
  כן מתקדמים. זו דוגמה חיה לדפוס שכבר תועד ב-`transient-failures-and-selftest.md`
  ("התג במסך לא עודכן... וזה שווה החלטה") — פשוט לא תוקן.

**מסקנה לעבודה קדימה:** הדוחות ב-`claude/*.md` הם **תיעוד של מה שנעשה ב-session
מסוים**, לא הבטחה שזה נמצא בקובץ הסופי ששרד. ל**פני** שמסתמכים על טענה מדוח —
**לגרוף את שם הפונקציה/הקבוע בפועל בקובץ הנוכחי.** אם המשתמש מעלה קובץ, זו
נקודת האמת היחידה; דוח שסותר אותו — הקובץ מנצח, והפער עצמו שווה ציון בפני
המשתמש (יש כאן עבודה אמיתית — כנראה תיקון תפוס-מיקרופון + הפרדת דרגי מודל —
שאולי אבדה או נמצאת בענף/session אחר שלא מוזג).

---

## 1. מה זה הפרויקט

קובץ HTML יחיד (`TrueTailor_CV_vXX_YY.html`, ~32,000 שורות) — אפליקציית "תפירת
קורות חיים" שרצה כולה בדפדפן של המשתמש: הוא מדביק את קורות החיים ואת תיאור
המשרה, האפליקציה קוראת ל-Gemini API עם **מפתח API אישי שהמשתמש מספק** (אין שרת
משלה, אין backend), מנתחת פערים, שואלת שאלות השלמה, כותבת קו"ח מותאם, ומייצאת
docx/PDF. תמיכה ב-12 שפות ממשק, מצב יום/לילה, ניגודיות גבוהה, RTL/LTR.

**העיקרון המכונן שחוזר בכל דוח:** שום דבר לא יוצא מהדפדפן חוץ מקריאות ל-Gemini
עם המפתח של המשתמש עצמו. זו הבטחת הפרטיות שכל שאר ההחלטות (בחירת ספק נגישות,
Gmail OAuth, מה נכנס ל-log) נשענות עליה. **אל תציע פתרון שדורש שרת אמצעי או
שליחת תוכן קו"ח/משרה לאן שהוא חוץ מ-Gemini עצמו.**

## 2. איך גרסאות עובדות

- `TT_VERSION` בקוד + שם הקובץ עולים יחד, סכימה `v<major>.<minor>.<patch>`
  (כרגע `v39.45.x`).
- כל שינוי מתועד כבלוק הערה בראש הקובץ, `v39.33→היום`, שורה לכל גרסה — **וזו גם
  מחרוזת חיפוש**: לחפש `v39.42` בקובץ מוצא את כל מה שהיא נגעה בו.
- לפני שמתחילים: לוודא מה הגרסה העדכנית ביותר בפועל (לא להניח לפי מספר בדוח ישן
  — לפחות פעמיים גרסה "עלתה" במספור שלא תאם את שם הקובץ, ראו `matching-questions-junior-mode-spec.md`).
- **דוח שינוי חדש נכתב לתוך הפרויקט** (claude/*.md) בסוף כל session משמעותי, באותו
  פורמט של הקבצים הקיימים: מה דווח/התבקש, מה נבדק בקוד לפני שתוקן (לא להניח),
  מה תוקן בפועל, איך נבדק, מה נשאר פתוח. זו לא בירוקרטיה — זה מה שמנע כפילות
  עבודה בין sessions (ראו `voice-report-verification-and-fixes.md`, שבו דוח
  חיצוני טען שלושה באגים ושניים מהם התבררו כלא-נכונים לגבי הקוד בפועל, ורק
  קריאה ישירה של הקוד מנעה תיקון-סרק).

## 3. מתודולוגיית הבדיקה — הכי חשוב בסקיל הזה

**אל תציג גרסה כגמורה בלי לעבור את הפרק הזה.** זו הדרישה המפורשת של המשתמש
בכל session, וגם הלקח החוזר ביותר בכל הדוחות: כל תיקון "בטוח" שלא נמדד בדפדפן
אמיתי התברר כשקרי לפחות פעם אחת.

### 3.1 סריקת דף טעון = חסרת ערך

הדף בנוי דינמית: 10-12 כרטיסים נושאים `tt-hidden` עד שריצה פותחת אותם, והם
**ריקים** עד ש-renderer כותב את גופם מ-`APP`. סריקת axe על הדף כפי שנטען תופסת
~10% מהאלמנטים. הדרך הנכונה: לבנות `APP` סינתטי מלא (signals, coverage, gaps,
verdict, chat turns, `APP.changes`) ולקרוא לכל ה-renderers (23 בקובץ), ואז לסרוק.

### 3.2 Tailwind חייב להיות בנוי מקומית לפני בדיקה

הקובץ טוען Tailwind מ-CDN. בסביבת בדיקה בלי רשת כל מחלקת Tailwind אינרטית —
הדף "נמדד נקי" בזמן שהוא בפועל בלתי קריא. **חובה:**
```
npx tailwindcss -c tw.config.js -i tw.css -o tw.full.css   # content: הקובץ עצמו
```
ואז להזריק את `tw.full.css` (`page.addStyleTag`) לפני כל סריקת ניגודיות/מבנה.
בלי זה כל מספר ניגודיות בדוח הוא בדיה.

### 3.3 שתי שיטות מדידה במקביל לניגודיות

`getComputedStyle` (מדויק אבל צריך הרכבה נכונה של שכבות שקופות/גרדיאנטים) +
קריאת פיקסלים מצילום מסך אמיתי (אמת קרקע, נופל על חפיפות/גליפים). מסקנה
נחשבת רק כששתיהן מסכימות.

### 3.4 `?selftest=1` — סוויטת בדיקות טהורות

בדיקות שרצות בלי מפתח API, בלי רשת, בלי DOM — פונקציות טהורות בלבד (parser,
wordDiff, WAV header, נרמול אודיו, rankModel, ttMapLimit, מוצא תפר OCR וכו').
**עיקרון מוצהר:** בדיקה שדורשת מפתח API היא בדיקה שאיש לא מריץ, וזו גרועה
מאין-בדיקה כי היא נראית ככיסוי. כל באג חדש שנתפס ראוי לשורת assertion לפני
שהוא נסגר — הסוויטה גדלה עם כל דוח (18 → 21 → 24 → 27...).

**כלל לכתיבת בדיקה:** לבדוק את **מה שהפונקציה מבטיחה**, לא מספר שהבדיקה עצמה
שחזרה. פעמיים בפרויקט בדיקה "נכשלה" והקוד היה תקין — כי הבדיקה בדקה את הניחוש
שלה ולא את החוזה של הפונקציה.

### 3.5 הוכחת-תפיסה של רשת הביטחון (Regression proof)

אחרי כתיבת בדיקה חדשה: לסמלץ בכוונה רגרסיה (לבטל את התיקון, "לפשט" פונקציה
בחזרה) ולוודא שהבדיקה המתאימה **נופלת**, ואז להחזיר ולוודא שהיא חוזרת לעבור.
בדיקה שלא נופלת כשמזריקים לה כשל אמיתית — לא בודקת כלום. (מלכודת שנמצאה: הזרקת
CSS שבור לא הפילה בדיקה כי ID ניצח את המחלקה שהוזרקה — לא כשל בדיקה, הזרקה
שלא ניצחה.)

### 3.6 סדר בדיקה מומלץ למשימה חדשה

1. `node --check` על בלוקי ה-script.
2. טעינה מלאה ב-Playwright/Chromium, אפס `pageerror`, אפס מחרוזות חסרות (`ttTxMissing()`).
3. Harness ממוקד לפיצ'ר שתוקן (DSP מסונתז, קסקדת מודלים מזויפת, וכו').
4. מעבר על 12 השפות: אין דליפת עברית, כל placeholder שרד.
5. אם נגעו בעיצוב/ניגודיות: 3.1-3.3 לעיל.
6. הוספת שורת assertion ל-`?selftest=1` + regression proof (3.5).
7. לציין בדוח **מה כן נבדק ומה לא** — לא להתיימר לכיסוי שלא היה.

## 4. מוסכמות קוד וארכיטקטורה

- **קידומת `tt`/`TT_`** לכל פונקציה/קבוע גלובלי של הפרויקט (`ttVoiceHandle`,
  `TT_VERSION`, `TT_TX`...).
- **i18n מרכזי:** `TT_TX[key][lang]`, 12 שפות (`he, ar, en, fr, es, de, it, pt, hr, ru, ja, hi`),
  ניגש דרך `H('key')`. `H()` נופל בחזרה לעברית כשחסר — טוב למניעת קריסה, רע כי
  זה מסתיר מחרוזת שנשכחה. **הבדיקה הנכונה לגילוי חוסרים:** לחלץ regex את כל
  `H('...')` מהקוד ולוודא שלכל אחת יש מפתח ב-`TT_TX` — לא לסמוך על `ttTxMissing()`
  שבודק רק תאים ריקים בשורות קיימות.
  - **מסמכים משפטיים (תנאים/פרטיות/נגישות) אינם ב-TT_TX בכוונה.** רק עברית
    ואנגלית מלאות; שאר 10 השפות מקבלות אנגלית + הבהרה מתורגמת "הנוסח העברי
    מחייב". חוזה מתורגם-מכונה שלא נבדק הוא סיכון גדול יותר מחוזה בדוק אחד +
    הבהרה. **אל תציע לתרגם אותם ל-12 שפות.**
  - **טקסט בתוך SVG לא נתפס ע"י `ttTxMissing()`** — נבנה כמחרוזת, לא נקרא מהטבלה.
    לחפש עברית בתוך `<text>` ב-SVG בקוד כבדיקה נפרדת.
- **מצב לילה / ניגודיות גבוהה:** שני מנגנונים על אותו CSS —
  1. טוקנים (`--c-ink`, `--c-paper`, `--c-mute`...) שמתהפכים עם `data-theme`.
  2. מחלקות Tailwind (`bg-white`, `text-slate-700`...) שנכתבו כליטרל ו**לא**
     מתהפכות.
  **כלל ברזל:** צבע שנכתב כליטרל (לא טוקן) לא יכול לענות לערכה — זה מקור כמעט
  כל באגי מצב הלילה בפרויקט (טקסט שנעלם, כפתור לבן-על-לבן, פאנל שלם שלא קיבל
  ערך כהה). כשמתקנים — **למפות את המחלקה עצמה** במקום עוד סלקטור נקודתי לפאנל
  ספציפי (תיקון פר-פאנל מתקן רק את מה שמישהו הסתכל בו).
  - `opacity` על טקסט **אינו** עמעום — הוא מיזוג לרקע וסוחר ניגודיות. לחפש כל
    שימוש ב-`opacity` על טקסט כחשוד תמידי.
  - `.tt-sheet`/`.tt-page`/`#printArea` (הגיליון המיוצא) **פטורים במכוון** ממצב
    לילה — הוא מדמה נייר מודפס ותואם ל-docx/PDF. אל תצבע אותו כהה. טוקנים
    שכן חייבים לחול עליו (למשל צ'יפ סטטוס) נבדקים מול **הערך המחושב** של מצב
    יום, לא מועתקים מהמקור — שתי רשימות נפרדות של אותם טוקנים הן איך שנוצר באג.
  - סלקטור ממוען **לפי מיקום** (`div:nth-of-type(6) > label > span`) נשבר בכל
    פעם שנוסף אלמנט מעליו. תמיד להוסיף `id` יציב לאלמנטי טקסט שהתרגום תלוי בהם.
  - `!important` על סלקטור רחב (`.tt-btn`) בולע את כל הווריאנטים (`-primary`,
    `-ok`, `-dark`, `-soft`) — התיקון הוא רשימת החרגה `:not()`, לא כלל נוסף
    שמתחרה בעדיפות.
- **בחירת מודל Gemini — המצב בפועל בקובץ (ראו §0 — לא TT_TIER, זה בוטל/לא מוזג):**
  - `ttMeasureCall` — מכשיר אחד נזכר (pinned) לשני שלבי הציון, כדי שהציון יהיה
    בר-השוואה בין ריצות. אין מקבילה מתועדת ומאומתת בקוד לנעילה דומה עבור שלב
    השאלות — לבדוק בפועל לפני שמניחים שהיא קיימת.
  - `CASCADE_STATUS = {400:false, 403:true, 404:true, 429:true, 500:true, 503:true, 504:true}`
    — סטטוסים עם `true` עוברים למודל הבא בקסקדה מיד.
  - `TT_RETRY_SAME` (ראו `transient-failures-and-selftest.md`) — ניסיון חוזר
    **בודד** לאותו מודל על 429/500/503 לפני מעבר למודל הבא, בלי כיבוד
    `Retry-After`/`retryDelay` מהתשובה. תקלה רגעית שנייה עדיין מפילה למודל חלש
    יותר — זה עדיין רשום כפגם איכות פתוח, לא תוקן במלואו.
  - `thinkingBudget` (דור Gemini ≤2.5, מספר) מול `thinkingLevel` (דור 3+, מילה:
    `low/medium/high`, **לא** `minimal` — נבדק בקוד: `ttThinkConfigFor` ממפה ל-`'low'`)
    — **אף פעם לא שניהם באותו אובייקט**. `ttThinkConfigFor(modelId, opts)` בוחר
    שדה לפי `rankModel(id) >= 30`, ותומך במודל שעוד לא קיים (`gemini-4`) בלי שינוי קוד.
  - **קריאות מקבילות מותרות רק כשאין תלות ביניהן**, ורק דרך `ttMapLimit`
    (תקרת מקביליות + סדר פלט לפי סדר קלט) — לא `Promise.all` גולמי, כי מכסת
    ה-API נספרת לדקה ומספר קריאות גדול במקביל נכשל בשקט (מודל חלש עונה על
    חלק מהעבודה).
  - **גלובלים שנקראים אחרי `await`** (`TT_LAST_MODEL`, `TT_LAST_TRUNCATED`) הם
    race condition ברגע שיש מקביליות. הפתרון הקבוע: `opts.meta` — הקורא מעביר
    אובייקט, `callGeminiRaw` כותב אליו סינכרונית לפני שהוא חוזר. **כל קורא
    גלובל חדש שנכתב מאז — חשוד עד שהומר.**
- **מסמך מיוצא (docx/PDF) הוא שכפול-שלוש:** אותה לוגיקת רינדור/מדידה כתובה
  בנפרד לתצוגה, לגיליון ההדפסה, ול-docx. שינוי בעיצוב חייב להיבדק בשלושתם
  (ראו `dark-mode-diff-marks-and-paper.md`: כללי `.tt-ins`/`.tt-del` בגיליון
  הייצוא הם מחרוזת נפרדת ולא נגעו בה בכוונה).
- **המסמך (קו"ח) לעולם לא נכתב מחדש בלי אישור מפורש בשלב 5.** כל שלב מאוחר
  יותר (עמוד אחד, פריסה צפופה) שמדגים "אני לא מוסיף/מנסח מחדש טקסט" חייב
  לעמוד בזה ממש — ה-DOM data-idx נשמר, `applyPreviewEdits` עדיין קורא שורה-שורה.
- **מדידת עמודים חייבת לקרות ברוחב A4 קבוע**, גם בטלפון — `max-width:100%` על
  `.tt-page` הוא באג נכונות (לא רק תצוגה) כי אותו אלמנט הוא מכשיר המדידה
  ל-`fitPageToLimit`. תיקון: רוחב A4 קבוע + `transform:scale()` ל-fit ויזואלי
  — ותמיד להסיר את ה-zoom לפני מדידה ולהחזיר אחרי.
- **פרטיות ב-logging:** `TT_LOG` לעולם לא רושם תוכן קו"ח/משרה/תשובות/מפתח.
  `shape()` מתאר ערכים במקום להעתיק (`"str(2841)"`, `{arr:14}`), ו-`safe()`
  מנקה מפתחות/טוקנים/מיילים/טלפונים מכל מחרוזת. כל תוספת ל-log חדשה צריכה
  לעבור דרך שתי השכבות האלה.

## 5. החלטות מוצר/משפט שכבר הוכרעו — לא לפתוח מחדש בלי סיבה חדשה

- ברירת מחדל: **עד שני עמודים**; מצב עמוד-אחד הוא הרחבה, שכבה נפרדת שכיבוי
  שלה מחזיר את המסמך בדיוק כפי שהיה.
- סדר עדיפות חיתוך בעמוד אחד (מהחלש למוגן): סעיפים נלווים לא-רלוונטיים →
  תפקידים ישנים (5-6+ שנים, בלי הוכחת דרישה) → בולטים לא-רלוונטיים בתפקיד
  אחרון → תקציר (עד 3-4 בולטים) → כישורים → **תארים ושפות/שירות צבאי מוגנים
  לחלוטין, אף פעם לא יורדים**.
  - שורה שמוכיחה דרישה ייחודית (`APP.coverage`) — חסינה תמיד.
  - קורס/סעיף נלווה שמשרת את המשרה (`ttServesPosting`, כל מונח מרשימת מילות
    המפתח בכל דרגת חשיבות) — **לא מוצע בכלל להסרה**.
  - כל תפקיד משאיר לפחות שתי שורות, אחרת מוסר כולו.
  - ללא ניסוח מחדש/מיזוג טקסטואלי בשלב החיתוך — הסרה בלבד. "פריסה" (שפות/צבא/
    כישורים על שורה אחת) הותרה כי אינה מוחקת אף תו, רק פורסת אחרת.
  - אזהרת ניסיון: מעל 12 שנות ניסיון מציגה הערה חד-פעמית שעמוד אחד אולי לא
    מתאים — לא חוסמת, לא חוזרת בכל לחיצה.
- מודלים חלשים לא מוגבלים בעלות בלבד — `ttLiteCascade` דורג לפי (עלות **וגם**
  גרסה): `rankModel` ממיין קודם לפי גרסה, כך שהיפוך נאיבי של הרשימה מוציא
  את המודל הישן-והיקר ביותר (`pro`) לראש "הזול". `ttCostRank` (lite→flash→
  ללא-תיוג→pro) הוא הכלל הנכון, ו-pro מוצא לגמרי מקסקדת ה-utility.
- Gmail: ברירת מחדל **טיוטה, לא שליחה** (`gmail.compose`, לא `gmail.send`) —
  שום דבר לא יוצא בלי שהמשתמש הסתכל.
- רכיב נגישות נכתב **עצמאית ולא הותקן כתוסף צד-שלישי**, כי תוסף מסחרי פונה
  לשרת חיצוני וסותר את הבטחת "שום דבר לא יוצא מהדפדפן". הצהרת הנגישות אומרת
  "חלקית" ולא "AA מלאה", כי לא נבדק ידנית עם קורא מסך (הפריט המהותי הפתוח
  היחיד ברשימת הנגישות).
- **פתוח, דורש החלטה של קובי (לא להכריע לבד):** ישות משפטית לפרסום, תיבת דואר
  נפרדת, סימן מסחר לשם, Google Fonts מול GDPR, בדיקת קורא-מסך ידנית.

## 6. תבניות באג חוזרות — לחפש אותן יזום כשעובדים באזור קרוב

מהדוחות: **אותה משפחת טעות חוזרת שוב ושוב במקומות שונים** — "צבע/ערך שנבחר
בהנחה שרק דבר אחד קורה, במקום שהערכה/המקביליות/גודל-המסך לא יכולים להגיע
אליו". כשנוגעים בקוד סמוך לאחד מהם, שווה לבדוק את כל המשפחה:

1. **ליטרל שצריך היה להיות טוקן** (צבע קשיח שלא עונה למצב לילה/HC).
2. **סלקטור לפי מיקום/מבנה** שנשבר כשמישהו מוסיף אלמנט מעליו — עדיף `id`.
3. **גלובל שנקרא אחרי `await`** — לא בטוח ברגע שיש שתי קריאות באוויר יחד.
4. **מכסת/פרמטר API ששונה בין דורות מודל** בלי שהקוד בדק את שניהם (thinking
   budget/level, שדה שהמודל דוחה) — נפילה חיננית שמסתירה 400 אבל גם מסתירה
   שהתיקון בכלל לא הופעל.
5. **מדידת גודל/מסך שמניחה סביבה אחת** (דסקטופ בלבד, ללא זום, ללא הגדלת גופן
   נגישה) בזמן שהאלמנט הזה גם מכשיר מדידה אמיתי (עמודים, ניגודיות).
6. **בדיקה שנכשלת אבל הקוד תקין** — לפני שמתקנים "כשל", לוודא שהבדיקה בודקת
   את חוזה הפונקציה ולא ניחוש עצמי.
7. **תלונת משתמש "X לא עובד" שהיא בעצם מגבלת פלטפורמה** (למשל `mailto:` בלי
   מטפל מוגדר) — לבדוק תחילה אם הקוד באמת תקין (Playwright אמיתי, לא הנחה),
   ואם כן, לשפר את **חוויית הכישלון** (למשל: העתקה ללוח + הודעה ליד הקישור)
   במקום "לתקן" קוד שכבר תקין.
8. **פאנל רשימה שהכפתורים היחידים להמשך יושבים בתוך אותו בלוק שנעלם כשהרשימה
   מתרוקנת** (ראו `answer-reuse-dead-end.md`, v39.45.3: הסרת הפריט האחרון
   מ"על אלה כבר ענית פעם" מחקה גם את "אשר והמשך"/"שאל אותי על הכל" - שני
   הכפתורים היחידים שקוראים ל-`askNextQuestion()` - ומשאירה מבוי סתום אמיתי,
   לא רק תצוגה ריקה). כל פאנל דינמי בקובץ שמרנדר `if (!list.length) { el.innerHTML
   = ''; return; }` **ובתוך אותו `innerHTML` גם יושבים פקדי ההמשך** חשוד באותה
   צורה - כדאי לבדוק ספציפית מה קורה כשמרוקנים אותו לגמרי, לא רק הסרה חלקית
   (בדיקת "הסרת פריט אחד מתוך כמה" עברה כאן בהצלחה ועדיין הסתירה את הבאג).
   פאנל הטרים (`renderTrimPanel`) ופאנל הסקירה (שלב 5, "אשר הכל"/"דחה הכל")
   בנויים על אותה תבנית ולא נבדקו ספציפית מול התרחיש הזה.

## 7. איפה למצוא פירוט

כל דוח session שמור בפרויקט תחת `claude/*.md`. לפני עבודה על נושא ספציפי —
לחפש בפרויקט (`project_search`) לפי שם הפיצ'ר לפני שמתחילים, כדי לא לשכפל
אפיון/תיקון שכבר נעשה. נושאים עיקריים לפי קובץ:

| נושא | קובץ |
|---|---|
| מדיניות/פריסת עמוד אחד | `one-page-spec.md`, `one-page-policy.md`, `one-page-compact-layout.md` |
| שכבת נייד/A4 | `mobile-layer.md` |
| מצב לילה/ניגודיות (כל הסבבים) | `dark-mode-*.md`, `legal-and-accessibility.md` |
| נגישות (widget, WCAG) | `accessibility-widget.md`, `legal-and-accessibility.md` |
| i18n/קטלוג תרגומים | `i18n-catalogue-gaps.md` |
| משוב/אבחון/Gmail | `feedback-and-diagnostics.md`, `feedback-attachments-and-gmail.md` |
| סריקת צילום מסך (OCR) | `screenshot-scanning.md` |
| תמלול קולי | `voice-transcription-accuracy.md`, `voice-report-verification-and-fixes.md` |
| שכבת תחבורה מול Gemini / דרגי מודל | `model-selection-and-speed.md` (תואם קוד בפועל) · `gemini-transport.md` (⚠️ ראו §0 — לא נמצא בקובץ שנבדק) |
| תקלות חולפות + selftest | `transient-failures-and-selftest.md` |
| ציד באגים כללי | `bug-hunt-models-and-voice.md` |
| כניסה למפתח API / אונבורדינג | `api-key-onboarding-language.md` |
| שאלות התאמה + מצב ג'וניור | `matching-questions-junior-mode-spec.md` |
| תיקוני ממשק קטנים | `icon-fixes-and-onepage-highlight.md` |
| פאנל "כבר ענית פעם" (reuse) - מבוי סתום בהסרת הפריט האחרון | `answer-reuse-dead-end.md` |

---

## 8. איך לפתוח משימה חדשה על הפרויקט

1. `project_search` בפרויקט לפי מילות המפתח של הבקשה — לבדוק אם כבר טופל/נדון.
2. לזהות את הגרסה העדכנית בפועל של הקובץ (לא להניח) — **אם המשתמש צירף את קובץ
   ה-HTML עצמו, זו נקודת האמת. לגרוף (grep) בו את שמות הפונקציות/הקבועים שדוח
   רלוונטי מזכיר לפני שסומכים על הדוח** — ראו §0, שם דוח שלם התברר כלא-ממוזג.
   בלי הקובץ בפועל, להתייחס לכל טענת דוח כלא-מאומתת ולומר זאת למשתמש.
3. **לקרוא את הקוד הרלוונטי בפועל לפני שמתקנים** — לפחות שלוש פעמים בפרויקט
   דוח/תלונה חיצונית טענה באג שלא היה קיים בקוד בפועל (ראו §6, סעיף 7).
4. ליישם, עם עין לתבניות ב-§6 באזור שנגעו בו.
5. לבדוק לפי §3 — כולל regression proof לכל בדיקה חדשה.
6. לתעד: מה התבקש, מה נבדק לפני התיקון, מה תוקן, איך אומת, מה נשאר פתוח —
   ולשמור כדוח חדש בפרויקט (`claude/<שם-תיאורי>.md`).
7. לתת למשתמש הסבר קצר לכל המלצה (מה גרם להמליץ עליה) ולציין אם ההמלצה
   נוגעת/מתנגשת עם משהו אחר בפרויקט — לפי העדפות המשתמש.
